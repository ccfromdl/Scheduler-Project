package com.jp.springboot.controller;

import org.springframework.web.bind.annotation.*;

@RestController
@RequestMapping("api/v1/user")
public class PersonController {
//    @Autowired
//    PersonService personService;
//
//    @Autowired
//    Gson gson;

//    @GetMapping(value = "{id}")
//    public ResponseEntity<User> fetch(@PathVariable("id") long id) {
//        User user = personService.getById(id);
//        return new ResponseEntity<User>(user, HttpStatus.OK);
//    }
//
//    @PostMapping(value = "", produces = "application/json")
//    public ResponseEntity<String> save(@RequestBody User user) {
//        User saved = personService.save(user);
//        return new ResponseEntity<String>(gson.toJson(saved), HttpStatus.CREATED);
//    }
}
